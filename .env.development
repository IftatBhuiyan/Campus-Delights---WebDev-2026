# Use the deployed API during local frontend development (same as GitHub Pages).
# To use a local backend instead, create .env.local with:
# VITE_API_URL=http://localhost:5001
VITE_API_URL=https://campus-delights-api.onrender.com
